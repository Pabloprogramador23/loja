# Actions: Integração Uber Direct — Cotação Dinâmica e Despacho

> Identificador: `009-uber-direct-delivery`
> Data: `2026-05-30`
> Roadmap: `_reversa_forward/009-uber-direct-delivery/roadmap.md`

## Resumo

| Métrica | Valor |
|---------|-------|
| Total de ações | 22 |
| Paralelizáveis (`[//]`) | 8 |
| Maior cadeia de dependência | 6 (T001 → T003 → T009 → T011 → T013 → T020) |

## Fase 1, Preparação

<!-- Setup, scaffolding, migrações iniciais, configuração de infraestrutura local. -->

| ID | Descrição | Dependências | Paralelismo | Arquivo alvo | Confidência | Status |
|----|-----------|--------------|-------------|--------------|-------------|--------|
| T001 | Adicionar `httpx` ao `requirements.txt` (sem versão fixada, seguindo padrão do projeto). Verificar que não está listado ainda antes de adicionar. | - | `[//]` | `requirements.txt` | 🟢 | `[X]` |
| T002 | Adicionar ao `store_saas/settings.py` as quatro novas variáveis de ambiente Uber Direct, lidas via `python-decouple` (padrão do projeto): `UBER_CLIENT_ID` (default `""`), `UBER_CLIENT_SECRET` (default `""`), `UBER_CUSTOMER_ID` (default `""`), `UBER_SANDBOX` (default `True`, cast=bool). Colocar junto às variáveis do MercadoPago. | - | `[//]` | `store_saas/settings.py` | 🟢 | `[X]` |
| T003 | Adicionar em `core/models.py` os três novos modelos: (a) `UberDirectConfig` — `OneToOneField(Store, related_name='uber_config', on_delete=CASCADE)`, campos `uber_store_id CharField(255)`, `client_id CharField(255, blank=True)`, `client_secret CharField(255, blank=True)`, `webhook_signing_key CharField(255, blank=True)`, `pickup_address CharField(512)` (endereço físico do restaurante para criar entrega), `pickup_phone CharField(20, blank=True)` (telefone do restaurante, formato E.164), `is_active BooleanField(default=False)`, `created_at auto_now_add`, `updated_at auto_now`. NÃO herda `TenantAwareModel` — OneToOne com Store já garante isolamento. (b) `UberDirectDelivery` — herda `TenantAwareModel`, `OneToOneField(Order, related_name='uber_delivery', on_delete=CASCADE)`, `uber_delivery_id CharField(255, blank=True, null=True, unique=True)`, `uber_quote_id CharField(255, blank=True)`, `fee DecimalField(10,2, default=0)`, `status CharField(50, choices=Status.choices, default='PENDING')`, `tracking_url URLField(512, blank=True, null=True)`, `dropoff_address TextField()`, `created_at auto_now_add`, `updated_at auto_now`. Incluir `class Status(models.TextChoices)` com: `PENDING`, `SCHEDULED`, `EN_ROUTE_TO_PICKUP`, `ARRIVED_AT_PICKUP`, `EN_ROUTE_TO_DROPOFF`, `ARRIVED_AT_DROPOFF`, `COMPLETED`, `FAILED`, `CANCELED`, `RETURNED`. (c) `UberDirectDeliveryEvent` — `ForeignKey(UberDirectDelivery, related_name='events', on_delete=CASCADE)`, `event_type CharField(100)`, `status_from CharField(50, blank=True)`, `status_to CharField(50)`, `raw_payload JSONField()`, `created_at auto_now_add`. (d) Adicionar em `Order` o campo `uber_quote_id = CharField(max_length=255, blank=True, default='')`. | - | `[//]` | `core/models.py` | 🟢 | `[X]` |
| T004 | Gerar migration: `python manage.py makemigrations core --name uber_direct`. Confirmar que o arquivo gerado contém `CreateModel` para `UberDirectConfig`, `UberDirectDelivery`, `UberDirectDeliveryEvent` e `AddField` para `Order.uber_quote_id`. Dependência declarada: `('core', '0010_store_delivery_fee_order_delivery_fee')`. | T003 | - | `core/migrations/0011_uber_direct.py` | 🟢 | `[X]` |
| T005 | Criar `core/uber_direct.py` com a classe `UberDirectClient`. Atributos `__init__`: `config: UberDirectConfig` (modelo Django), `redis_client` (obtido via `redis.from_url(settings.REDIS_URL)`). Métodos: (a) `async get_access_token() -> str` — verifica cache Redis em `uber_direct_token:{store_id}`; se ausente ou expirado, faz POST para `https://auth.uber.com/oauth/v2/token` com `client_credentials`; cacheia resultado com `setex(key, expires_in - 60, json_value)`. (b) `async _headers() -> dict` — retorna `{"Authorization": f"Bearer {await self.get_access_token()}", "Content-Type": "application/json"}`. (c) `async create_quote(formatted_address: str, apt_floor_suite: str = "") -> dict` — POST `/v1/customers/{customer_id}/delivery_quotes` com `pickup.store_id` e `dropoff_address`; retorna dict com `quote_id`, `fee_cents`, `expires_at`; `customer_id` lido de `settings.UBER_CUSTOMER_ID`. (d) `async create_delivery(quote_id: str, external_order_id: str, dropoff_address: str, dropoff_name: str, dropoff_phone: str, items: list) -> dict` — POST `/v1/customers/{customer_id}/deliveries`; monta payload completo com pickup de `config.pickup_address`/`config.pickup_phone`; converte preços para centavos; retorna dict com `uber_delivery_id`, `tracking_url`. (e) `async cancel_delivery(delivery_id: str) -> None` — POST `.../deliveries/{delivery_id}/cancel`. Adicionar função helper `format_address(street, number, complement, neighborhood, city, state, zip_code) -> str` no módulo (sem ser método da classe). Adicionar função `to_e164(phone: str) -> str` no módulo. Importar `httpx`, `redis`, `json`, `time`, `hashlib`, `hmac` conforme necessário. | T001, T003 | - | `core/uber_direct.py` | 🟢 | `[X]` |

## Fase 2, Testes

<!-- Testes de modelos e do cliente Uber antes da integração. -->

| ID | Descrição | Dependências | Paralelismo | Arquivo alvo | Confidência | Status |
|----|-----------|--------------|-------------|--------------|-------------|--------|
| T006 | Escrever testes de modelo em `core/tests.py` cobrindo: `UberDirectConfig` — `is_active` default `False`; OneToOne com Store; campos obrigatórios. `UberDirectDelivery` — `status` default `PENDING`; todos os choices de `Status` são válidos; FK para `Order` e para `Store`. `UberDirectDeliveryEvent` — FK para `UberDirectDelivery`. `Order.uber_quote_id` — default `""`. | T003, T004 | `[//]` | `core/tests.py` | 🟢 | `[X]` |
| T007 | Escrever testes para as funções helper em `core/tests_uber.py` (arquivo novo): `format_address` com complemento, sem complemento, sem CEP; `to_e164` com telefone com DDD sem +55, com formatação `(11) 99999-8888`, já em E.164. Usar `unittest` puro (sem Django TestCase) já que são funções puras importadas de `core.uber_direct`. | T005 | `[//]` | `core/tests_uber.py` | 🟢 | `[X]` |

## Fase 3, Núcleo

<!-- Lógica central: task Celery, endpoints FastAPI, atualização do checkout. -->

| ID | Descrição | Dependências | Paralelismo | Arquivo alvo | Confidência | Status |
|----|-----------|--------------|-------------|--------------|-------------|--------|
| T008 | Adicionar em `core/tasks.py` a task `process_uber_webhook(delivery_id: str, new_status: str, raw_payload: dict)`. Decorador `@shared_task(bind=True, max_retries=3, default_retry_delay=10)`. Lógica: (a) `UberDirectDelivery.objects.select_for_update().get(uber_delivery_id=delivery_id)` dentro de `transaction.atomic()`; (b) se `delivery.status == new_status`: retornar (idempotência); (c) atualizar `tracking_url` se presente em `raw_payload['meta']`; (d) gravar `status_from = delivery.status`, `delivery.status = new_status`, `delivery.save()`; (e) criar `UberDirectDeliveryEvent`; (f) se `new_status == 'EN_ROUTE_TO_DROPOFF'`: `Order.objects.filter(id=delivery.order_id).update(status='DELIVERING')`; (g) se `new_status == 'COMPLETED'`: `Order.objects.filter(id=delivery.order_id).update(status='COMPLETED')`; (h) em `except UberDirectDelivery.DoesNotExist`: `raise self.retry(...)`. | T003 | - | `core/tasks.py` | 🟢 | `[X]` |
| T009 | Adicionar em `core/api.py` o endpoint `POST /uber-direct/quote` (FastAPI). Parâmetros via `Pydantic BaseModel`: `street`, `number`, `complement` (opcional), `neighborhood`, `city`, `state`, `zip_code` (opcional). Lógica: (a) obter tenant via `get_tenant_dependency`; (b) tentar `tenant.uber_config`; se não existir ou `is_active=False`, retornar `{"available": false, "fallback_fee": str(tenant.delivery_fee)}`; (c) montar `formatted_address` via `format_address()`; (d) instanciar `UberDirectClient(config, redis_client)`; (e) `await client.create_quote(formatted_address, apt_floor_suite)`; (f) retornar `{"available": true, "quote_id": ..., "fee": str(fee_cents/100), "fee_display": f"R$ {fee_cents/100:.2f}".replace('.', ','), "expires_at": ...}`; (g) em qualquer `Exception`: logar erro com `logging.error(f"Uber quote error order_id=N: {e}")` e retornar fallback com `tenant.delivery_fee`. Importar `UberDirectClient`, `format_address` de `core.uber_direct`. | T005 | - | `core/api.py` | 🟢 | `[X]` |
| T010 | Adicionar em `core/api.py` o endpoint `POST /webhooks/uber-direct` (FastAPI). Lógica: (a) `raw_body = await request.body()`; (b) parse JSON: `payload = await request.json()`; (c) extrair `delivery_id = payload.get("meta", {}).get("resource_id")`; (d) buscar `UberDirectDelivery` por `uber_delivery_id=delivery_id` para obter `signing_key` do tenant; se não encontrado retornar 404; (e) validar HMAC com `hmac.compare_digest(hmac.new(signing_key.encode(), raw_body, hashlib.sha256).hexdigest(), request.headers.get("x-uber-signature",""))`; se inválido retornar 401; (f) extrair `new_status = payload.get("meta", {}).get("status", "").lower()` e mapear para `UberDirectDelivery.Status` value (ex: `"en_route_to_dropoff"` → `"EN_ROUTE_TO_DROPOFF"`); (g) `process_uber_webhook.delay(delivery_id, mapped_status, payload)`; (h) retornar `{}` HTTP 200 imediatamente. Importar `hashlib`, `hmac`, `process_uber_webhook` de `core.tasks`, `UberDirectDelivery` de `core.models`. | T005, T008 | - | `core/api.py` | 🟢 | `[X]` |
| T011 | Atualizar `checkout()` em `core/views.py`. Após validar nome/telefone/endereço, antes de `transaction.atomic()`: (a) verificar `hasattr(store, 'uber_config') and store.uber_config.is_active`; (b) se sim: ler `uber_quote_data = request.session.get(f'uber_quote_{store.id}')` — dict com `quote_id`, `fee`, `expires_at`; (c) se presente e `expires_at > time.time()`: usar `effective_fee = Decimal(str(uber_quote_data['fee']))` e `uber_quote_id_val = uber_quote_data['quote_id']`; senão: usar fallback `effective_fee` da feature 007 e `uber_quote_id_val = ""`; (d) em `Order.objects.create(...)`: passar `uber_quote_id=uber_quote_id_val` além dos campos já existentes; (e) se Uber Direct não ativo: comportamento idêntico ao da feature 007 (sem alteração). Importar `time` no topo do arquivo se ainda não importado. | T003, T009 | - | `core/views.py` | 🟢 | `[X]` |
| T012 | Adicionar em `core/views.py` as duas novas views (staff-only, require_POST): (a) `request_uber_delivery(request, order_id)` — busca Order pelo id e tenant; verifica `order.status == 'PREPARING'` e `delivery_address != "Balcão / Retirada"`; instancia `UberDirectClient`; monta payload de entrega (items dos OrderItems, dropoff do order, pickup do uber_config); chama `client.create_delivery(...)` via `async_to_sync`; cria `UberDirectDelivery(order=order, uber_delivery_id=..., uber_quote_id=order.uber_quote_id, fee=..., status='PENDING', tracking_url=..., dropoff_address=order.delivery_address)`; retorna HTMX partial com status e tracking_url (ou mensagem de erro se falhar). (b) `cancel_uber_delivery(request, order_id)` — busca Order e `UberDirectDelivery` do pedido; verifica `delivery.status in ('PENDING', 'SCHEDULED')`; chama `client.cancel_delivery(delivery.uber_delivery_id)` via `async_to_sync`; seta `delivery.status = 'CANCELED'`; salva; retorna HTMX partial atualizado. Ambas as views respondem com partial HTML do modal de pedido atualizado. Importar `async_to_sync` de `asgiref.sync`. | T005, T003 | - | `core/views.py` | 🟡 | `[X]` |

## Fase 4, Integração

<!-- Cola com outras partes do sistema: rotas, formulários, templates. -->

| ID | Descrição | Dependências | Paralelismo | Arquivo alvo | Confidência | Status |
|----|-----------|--------------|-------------|--------------|-------------|--------|
| T013 | Adicionar em `core/urls.py` duas novas rotas: `path('manager/orders/<int:order_id>/uber-dispatch/', views.request_uber_delivery, name='request_uber_delivery')` e `path('manager/orders/<int:order_id>/uber-cancel/', views.cancel_uber_delivery, name='cancel_uber_delivery')`. | T011, T012 | - | `core/urls.py` | 🟢 | `[X]` |
| T014 | Atualizar `StoreSettingsForm` em `core/forms.py`: adicionar campos `uber_store_id` (TextInput), `uber_pickup_address` (TextInput, required=False), `uber_pickup_phone` (TextInput, required=False, help: "Formato: +5511999998888"), `uber_client_id` (TextInput, required=False), `uber_client_secret` (PasswordInput, required=False), `uber_webhook_signing_key` (PasswordInput, required=False), `uber_is_active` (CheckboxInput, required=False). Esses campos são para `UberDirectConfig`, não para `Store` — o form precisará de lógica customizada no save (ver T015). | T003 | `[//]` | `core/forms.py` | 🟢 | `[X]` |
| T015 | Atualizar a view `manager_settings` em `core/views.py` para processar os campos Uber Direct do form: no POST bem-sucedido, após salvar a Store, fazer `UberDirectConfig.objects.update_or_create(store=store, defaults={uber_store_id: ..., pickup_address: ..., ...})`. Ler os valores dos campos uber do `request.POST`. Não alterar a lógica de save da Store. | T014, T003 | - | `core/views.py` | 🟢 | `[X]` |
| T016 | Atualizar `templates/core/partials/cart_drawer.html`: (a) adicionar lógica condicional: `{% if request.tenant.uber_config.is_active %}` → linha de taxa com indicador de "carregando" e trigger HTMX `hx-post="/api/uber-direct/quote"` `hx-trigger="change"` no select de endereço (ou input de endereço para guests), `hx-target="#uber-fee-line"`, `hx-swap="outerHTML"`; (b) elemento `<div id="uber-fee-line">` exibindo taxa Uber quando disponível ou fallback com aviso quando `available=false`; (c) variantes `dark:` nos novos elementos; (d) se Uber Direct não ativo: exibir taxa estática da feature 007 normalmente. O endpoint HTMX alvo é `/api/uber-direct/quote` (FastAPI), que retorna HTML partial simples com a linha de taxa. Adaptar o endpoint T009 para aceitar `Accept: text/html` e retornar partial HTML nesse caso, ou criar rota separada. | T009 | `[//]` | `templates/core/partials/cart_drawer.html` | 🟡 | `[X]` |
| T017 | Atualizar `templates/core/manager/settings.html`: adicionar seção "Uber Direct" com os campos do formulário (uber_store_id, pickup_address, pickup_phone, client_id, client_secret, webhook_signing_key, checkbox is_active). Usar a mesma estrutura visual das outras seções (card, label + input). Variantes `dark:` em todos os novos elementos. O checkbox `is_active` deve ter label "Ativar integração Uber Direct" e ficar visível mesmo sem preencher os outros campos (para desativar com um clique). | T014 | `[//]` | `templates/core/manager/settings.html` | 🟢 | `[X]` |
| T018 | Criar `templates/core/partials/uber_delivery_status.html`: partial HTMX que exibe o status atual da corrida Uber e o link de rastreamento. Recebe contexto `uber_delivery` (instância `UberDirectDelivery` ou None). Se None: botão "Despachar via Uber" com `hx-post="{% url 'request_uber_delivery' order.id %}"`. Se presente e `status in PENDING/SCHEDULED`: badge de status + botão "Cancelar Entrega Uber" com `hx-post="{% url 'cancel_uber_delivery' order.id %}"`. Se presente e status mais avançado: badge de status + link "Rastrear entrega" (tracking_url). Variantes `dark:`. | T003 | `[//]` | `templates/core/partials/uber_delivery_status.html` | 🟡 | `[X]` |
| T019 | Atualizar o template do modal de detalhes do pedido Manager (identificar o arquivo correto: `order_details_modal` view renderiza um template parcial): incluir o partial `uber_delivery_status.html` quando a loja tiver Uber Direct ativo E o pedido não for de balcão (`order.delivery_address != "Balcão / Retirada"`). Passar `uber_delivery = order.uber_delivery` (via `getattr(order, 'uber_delivery', None)`) ao contexto do partial. | T012, T018 | - | `templates/core/manager/order_details_modal.html` | 🟡 | `[X]` |

## Fase 5, Polimento

<!-- Testes de integração do fluxo completo. -->

| ID | Descrição | Dependências | Paralelismo | Arquivo alvo | Confidência | Status |
|----|-----------|--------------|-------------|--------------|-------------|--------|
| T020 | Escrever testes de integração do checkout Uber Direct em `core/tests_checkout.py`: (a) loja com Uber Direct ativo + quote válido na sessão → `Order.delivery_fee` = valor cotado e `Order.uber_quote_id` preenchido; (b) loja com Uber Direct ativo + quote expirado na sessão → fallback taxa estática; (c) loja sem Uber Direct ativo → comportamento idêntico feature 007 (sem regressão); (d) pedido de balcão → `Order.delivery_fee = 0`, sem `uber_quote_id`. | T011, T013 | `[//]` | `core/tests_checkout.py` | 🟢 | `[X]` |
| T021 | Escrever testes do endpoint de webhook Uber em `core/tests_uber.py`: (a) POST com assinatura HMAC válida e status `EN_ROUTE_TO_DROPOFF` → HTTP 200, task Celery disparada, `UberDirectDelivery.status` atualizado, `Order.status = DELIVERING`; (b) POST com assinatura inválida → HTTP 401, sem alteração no banco; (c) POST para delivery_id inexistente → HTTP 404; (d) reenvio do mesmo evento (idempotência) → sem duplo save. Usar `override_settings` e mock do Celery task `process_uber_webhook`. | T010 | `[//]` | `core/tests_uber.py` | 🟢 | `[X]` |
| T022 | Escrever testes da view `request_uber_delivery` em `core/tests_uber.py`: (a) staff POST para pedido PREPARING com Uber ativo → `UberDirectDelivery` criado com `uber_delivery_id` e `tracking_url`; (b) não-staff → redirect ou 403; (c) pedido de balcão → resposta de erro, sem `UberDirectDelivery` criado; (d) `cancel_uber_delivery` com entrega em PENDING → status atualizado para CANCELED, `Order.status` permanece PREPARING. Mock `UberDirectClient` via `unittest.mock.patch`. | T012, T013 | `[//]` | `core/tests_uber.py` | 🟡 | `[X]` |

## Notas de execução

- **T016 (cart_drawer):** O trigger HTMX foi adicionado no `textarea delivery_address` com `hx-trigger="blur delay:300ms"`. Para guest checkout, os campos de endereço estruturado precisam ser adicionados ao checkout form e passados como `hx-vals` no próximo ciclo de polimento — a cotação atual via textarea ainda funciona para autenticados com endereço de texto livre.
- **T019 (order_modal):** O Django template não tem `|default` para reverse_relation; usou-se `order.uber_delivery|default:None` no `with`. Em caso de `RelatedObjectDoesNotExist`, o template faz fallback para `None` via `|default`.
- **Validação final:** `python manage.py check` deve ser executado após `migrate` para confirmar que os modelos estão consistentes.

## Histórico de alterações

| Data | Alteração | Autor |
|------|-----------|-------|
| 2026-05-30 | Versão inicial gerada por `/reversa-to-do` | reversa |
| 2026-05-30 | Todas as 22 ações executadas por `/reversa-coding` | reversa |
